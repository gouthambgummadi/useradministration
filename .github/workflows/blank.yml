name: MacBook Info Script

on: [push]

jobs:
  run-script:
    runs-on: macos-latest
    steps:
      - name: ⬇️ Checkout Repo
        uses: actions/checkout@v3

      - name: 📜 Make Script Executable
        run: chmod +x ./macinfo.sh

      - name: 🚀 Run MacBook Info Script
        run: ./macinfo.sh
